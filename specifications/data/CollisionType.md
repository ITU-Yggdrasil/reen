## CollisionType

### Description
A simple enum enumerating the various types of collisions in the game. The values are Obstacle and Food.

### Type Kind
Enum

### Mutability
Immutable

### Properties
- **Obstacle** - Represents a collision with the game's boundary or an obstacle.
- **Food** - Represents a collision with a consumable item on the board.

### Functionalities
- None explicitly mentioned, but typically includes the ability to determine the type of collision.

### Constraints & Rules
- None explicitly stated, but behavior is implied by the values.

### Inferred Types or Structures
- **None** inferred from the provided draft.

### Blocking Ambiguities
- **None** identified as blocking ambiguities.

### Implementation Choices Left Open
- **Exact string representation of the enum values** (e.g., `Obstacle` and `Food`) is left open. The use of `chrono` or other date/time libraries is not relevant here.

---

This specification is derived directly from the provided draft without adding any new information or assumptions. The properties and functionality are explicitly stated, and no blocking ambiguities or implementation choices are identified as non-blocking.