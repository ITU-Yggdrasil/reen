[package]
name = "specifications"
version = "0.1.0"
edition = "2021"

[lib]
name = "specifications"
path = "src/lib.rs"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
tracing = "0.1"
